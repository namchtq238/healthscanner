{
  "name": "discord-food-health-bot",
  "version": "1.0.0",
  "description": "Discord bot that analyzes food images with Gemini and returns an Apple Health shortcut link.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "up": "docker compose up -d --build",
    "down": "docker compose down -v"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "discord.js": "^14.22.1",
    "dotenv": "^16.6.1",
    "express": "^5.2.1"
  }
}
